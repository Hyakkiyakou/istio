name: Docker Image CI

on:
  push:
    branches: [ "1.17" ]
  pull_request:
    branches: [ "1.17" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag istio/pilot:1.17
